#include<stdio.h>
int febo(int n)
{
    if(n==1 || n==2)
        return 1;
    else
        return febo(n-1)+febo(n-2);
}
int main()
{
    int n,r;
    scanf("%d",&n);
    r=febo(n);
    printf("%d\n",r);
    return 0;
}
